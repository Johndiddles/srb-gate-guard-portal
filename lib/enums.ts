export enum AdminRole {
  SUPER_ADMIN = "SUPER_ADMIN",
  RESORT_SECURITY = "RESORT_SECURITY",
  FRONT_DESK = "FRONT_DESK",
}

export enum LicenseStatus {
  UNUSED = "UNUSED",
  USED = "USED",
}

export enum GuestType {
  IN_HOUSE = "IN_HOUSE",
  ARRIVAL = "ARRIVAL",
}

export enum MovementType {
  GUEST = "GUEST",
  VEHICULAR = "VEHICULAR",
}

export enum MovementDirection {
  IN = "IN",
  OUT = "OUT",
}
